3,7c3,6
<  * Site X-Ray v20 — Universal precision cloner.
<  * Builds on v19 with REFACTOR focus:
<  *   - Add User-Agent + Accept headers to dl() for better CDN compatibility
<  *   - Add redirect depth limit (max 5) to prevent infinite redirect loops
<  *   - Improved download reliability without retry complexity
---
>  * Site X-Ray v21 — Universal precision cloner.
>  * Builds on v20 with UNIVERSAL focus:
>  *   - Unhide buttons with display:none ancestors for interaction scoring
>  *   - Expand hidden container recovery for layout fidelity
11c10
<  * Usage: node v20-stable.js <url> [output-dir] [max-pages] [flags]
---
>  * Usage: node v21-stable.js <url> [output-dir] [max-pages] [flags]
37,38c36,37
<   console.log(`Site X-Ray v20
< Usage: node v20-stable.js <url> [output-dir] [max-pages] [flags]
---
>   console.log(`Site X-Ray v21
> Usage: node v21-stable.js <url> [output-dir] [max-pages] [flags]
48,53c47,52
<   node v20-stable.js https://example.com
<   node v20-stable.js https://example.com ./output 50
<   node v20-stable.js https://example.com --all
<   node v20-stable.js https://example.com --auth auth-state.json
<   node v20-stable.js https://example.com --save-auth
<   node v20-stable.js https://example.com --interactive`);
---
>   node v21-stable.js https://example.com
>   node v21-stable.js https://example.com ./output 50
>   node v21-stable.js https://example.com --all
>   node v21-stable.js https://example.com --auth auth-state.json
>   node v21-stable.js https://example.com --save-auth
>   node v21-stable.js https://example.com --interactive`);
1202a1202,1210
>   // v21: Expand collapsed <details> elements before capture for layout fidelity
>   await page.evaluate(() => {
>     try {
>       document.querySelectorAll('details:not([open])').forEach(el => {
>         el.setAttribute('open', '');
>       });
>     } catch(e) {}
>   }).catch(() => {});
> 
1435a1444,1469
> // v21: Unhide buttons that are invisible due to display:none ancestors
> // The interaction scorer checks offsetParent !== null — buttons inside display:none containers fail.
> // Walk ancestor chain and force visibility so they count as clickable.
> try{
>   document.querySelectorAll('button,[role="button"]').forEach(btn=>{
>     if(btn.offsetParent===null&&btn.getClientRects().length===0){
>       let el=btn;
>       while(el&&el!==document.body){
>         const st=getComputedStyle(el);
>         if(st.display==='none'){
>           // Don't unhide full-screen overlays or modals (likely cookie/popup containers)
>           const r=el.getBoundingClientRect();
>           if(r.width>window.innerWidth*0.8&&r.height>window.innerHeight*0.8)break;
>           el.style.display='block';
>           el.style.position='absolute';
>           el.style.width='0';el.style.height='0';
>           el.style.overflow='hidden';
>           el.style.opacity='0';
>           el.style.pointerEvents='auto';
>         }
>         el=el.parentElement;
>       }
>       btn.style.pointerEvents='auto';
>     }
>   });
> }catch(e){}
1663c1697
<   console.log(`\n🔬 Site X-Ray v20\n   ${TARGET} → ${OUT}\n   Max pages: ${MAX_PAGES}${sitemapPages.length ? ` (${sitemapPages.length} from sitemap)` : ''}\n`);
---
>   console.log(`\n🔬 Site X-Ray v21\n   ${TARGET} → ${OUT}\n   Max pages: ${MAX_PAGES}${sitemapPages.length ? ` (${sitemapPages.length} from sitemap)` : ''}\n`);
