3,6c3,7
<  * Site X-Ray v47 — Universal precision cloner.
<  * Builds on v46:
<  *   - PIXEL FIX: Set scroll-driven --progress to 1 (not 0) to reveal content
<  *   - Previous: SVG fragment fix, CSS/GSAP animation freeze
---
>  * Site X-Ray v48 — Universal precision cloner.
>  * Builds on v47:
>  *   - PIXEL FIX: Skip overlay elements in opacity reveal to avoid covering content
>  *   - PIXEL FIX: Remove max-height on --progress elements (clips card grids)
>  *   - Previous: --progress=1, SVG fragment fix, CSS/GSAP animation freeze
10c11
<  * Usage: node v47-stable.js <url> [output-dir] [max-pages] [flags]
---
>  * Usage: node v48-stable.js <url> [output-dir] [max-pages] [flags]
36,37c37,38
<   console.log(`Site X-Ray v47
< Usage: node v47-stable.js <url> [output-dir] [max-pages] [flags]
---
>   console.log(`Site X-Ray v48
> Usage: node v48-stable.js <url> [output-dir] [max-pages] [flags]
47,52c48,53
<   node v47-stable.js https://example.com
<   node v47-stable.js https://example.com ./output 50
<   node v47-stable.js https://example.com --all
<   node v47-stable.js https://example.com --auth auth-state.json
<   node v47-stable.js https://example.com --save-auth
<   node v47-stable.js https://example.com --interactive`);
---
>   node v48-stable.js https://example.com
>   node v48-stable.js https://example.com ./output 50
>   node v48-stable.js https://example.com --all
>   node v48-stable.js https://example.com --auth auth-state.json
>   node v48-stable.js https://example.com --save-auth
>   node v48-stable.js https://example.com --interactive`);
1588c1589
<   // v47: PIXEL FIX — Set scroll-driven --progress to 1 (fully revealed) before DOM capture.
---
>   // v48: PIXEL FIX — Reveal scroll-driven content before DOM capture.
1590,1592c1591,1593
<   // At domcontentloaded+4s (when the test snapshots), JS has set --progress to ~1 (loaded state).
<   // v44 incorrectly reset to 0, which collapsed scroll-driven rows (e.g. ingamana thumbnails).
<   // Setting to 1 matches the "fully loaded" visual state the test captures.
---
>   // Setting --progress to 1 matches the "fully loaded" visual state the test captures.
>   // Also remove max-height constraint — with --progress=1 it's redundant and clips card grids
>   // when --base-height was captured at a small value.
1597a1599,1600
>           // v48: Remove max-height constraint — it clips scroll-reveal content
>           el.style.setProperty('max-height', 'none', 'important');
2005a2009,2011
>         // v48: Skip absolute/fixed positioned overlays with z-index > 1 (intentional covers)
>         const pos=cs.position;const zi=parseInt(cs.zIndex);
>         if((pos==='absolute'||pos==='fixed')&&zi>1)return;
2252c2258
<   console.log(`\n🔬 Site X-Ray v47\n   ${TARGET} → ${OUT}\n   Max pages: ${MAX_PAGES}${sitemapPages.length ? ` (${sitemapPages.length} from sitemap)` : ''}\n`);
---
>   console.log(`\n🔬 Site X-Ray v48\n   ${TARGET} → ${OUT}\n   Max pages: ${MAX_PAGES}${sitemapPages.length ? ` (${sitemapPages.length} from sitemap)` : ''}\n`);
2655a2662,2664
>         // v48: Skip absolute/fixed positioned overlays with z-index > 1
>         const pos=cs.position;const zi=parseInt(cs.zIndex);
>         if((pos==='absolute'||pos==='fixed')&&zi>1)return;
