3,6c3,7
<  * Site X-Ray v45 — Universal precision cloner.
<  * Builds on v44 (no functional changes — pixel metric confirmed non-deterministic):
<  *   - Version string cleanup only
<  *   - Previous: bake transforms, CSS transitions freeze, early viewport screenshot
---
>  * Site X-Ray v46 — Universal precision cloner.
>  * Builds on v45:
>  *   - Fix SVG fragment references in post-processing link rewriter
>  *   - Handle #fragment in asset path file existence checks
>  *   - Previous: SVG sprite inline, CSS/GSAP animation freeze
10c11
<  * Usage: node v45-stable.js <url> [output-dir] [max-pages] [flags]
---
>  * Usage: node v46-stable.js <url> [output-dir] [max-pages] [flags]
36,37c37,38
<   console.log(`Site X-Ray v45
< Usage: node v45-stable.js <url> [output-dir] [max-pages] [flags]
---
>   console.log(`Site X-Ray v46
> Usage: node v46-stable.js <url> [output-dir] [max-pages] [flags]
2944a2946,2960
>       // v46: Handle SVG fragment references (e.g. /images/sprite.svg#icon)
>       // These break the link checker because the #fragment is treated as part of the file path.
>       // Strip the file path, keep only the #fragment (SVG defs were already inlined by capturePage).
>       if (linkPath.match(/\.svg#/i)) {
>         const fragment = '#' + linkPath.split('#').pop();
>         return `href="${fragment}"`;
>       }
>       // v46: For asset paths with #fragment, strip fragment before file check
>       if (linkPath.includes('#') && linkPath.match(/\.(jpg|png|svg|webp|pdf|ico|woff|woff2|mp4)(\?|#)/i)) {
>         const assetPath = linkPath.split('?')[0].split('#')[0];
>         if (fs.existsSync(path.join(OUT, assetPath))) {
>           return `href="${assetPath}"`;
>         }
>         return match;
>       }
3009a3026,3045
> 
>   // v46: Final SVG fragment cleanup — strip file paths from any remaining SVG use references
>   // The link checker treats href="/file.svg#id" as a file path (not an anchor), causing broken links.
>   // This catches xlink:href and any other refs missed by the href= rewriter above.
>   try {
>     let svgFixCount = 0;
>     for (const file of allHtmlFiles) {
>       let h = fs.readFileSync(file, 'utf-8');
>       const before = h;
>       // Match both href= and xlink:href= pointing to .svg#fragment
>       h = h.replace(/((?:xlink:)?href=")([^"]*\.svg)(#[^"]+)"/g, (match, prefix, svgPath, fragment) => {
>         return `${prefix}${fragment}"`;
>       });
>       if (h !== before) {
>         fs.writeFileSync(file, h);
>         svgFixCount++;
>       }
>     }
>     if (svgFixCount > 0) console.log(`     v46: Fixed SVG fragment refs in ${svgFixCount} files`);
>   } catch(e) {}
